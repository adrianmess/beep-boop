var array =  [1, 2, 3, 0, 4]

var div = function (three) {
   return (three % 3 ===0)};
index = array.findIndex(div);
array.splice(index,1,'dave');


==================================================================

array =  [1, 2, 3, 0, 4, 3]

var div = function (three) {
   return (three % 3 ===0)};
 if (index = array.findIndex(div));

array.splice(index,1,'dave');
}
console.log(array)


============================FOR undeited======================================

var array =  [1, 2, 3, 0, 4, 3]

var div = function (three) {
   return (three % 3 ===0)};

for (i = 0; i < array.length; i++); {
index = array.findIndex(div);
array.splice(index,1,'dave');
}
console.log(array)

===================================-FOR In Progress===============================
var array =  [1, 2, 3, 0, 4, 3]

var div = function (three) {
   return (three % 3 ===0)};

forEach (i = 0; i < array.length; i++); {
 
}
console.log(i)

============================FOR EACH working======================================
var array =  [1, 2, 3, 0, 4, 3]

var div = function (three) {
   return (three % 3 ===0)};

array.forEach(function (i) {
index = array.findIndex(div) 
// if i = div {
	array.splice(index,1,'dave');

// 	if (index = array.findIndex(div));
//  (i = 0; i < array.length; i++); {
//  if (index = array.findIndex(div));

// array.splice(index,1,'dave');
// }
console.log(index)
});


(6) [1, 2, "dave", "dave", 4, "dave"]
============================FOR EACH In Progression======================================
var array =  [1, 2, 3, 0, 4, 3]
var div = function (three) {
   return (three % 3 ===0)};

array.forEach(function (i) {
index = array.findIndex(div) 
	array.splice(index,1,'dave');
	console.log(index);
});


(6) [1, 2, "dave", "dave", 4, "dave"]
